#include "main.h"
#include <string.h>

/**
 * reverse_array - reverses the elements of array
 * @a: a parameter
 * @n: number of characters
 *
 * Return: void
 */
void reverse_array(int *a, int n)
{
	strrev(a, n);

	return (0);
}
