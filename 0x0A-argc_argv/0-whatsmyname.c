#include <stdio.h>

/**
 * main - writes the name of the program
 * @argc: the number of arguments in commandline
 * @argv: the array of args in commandline
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
