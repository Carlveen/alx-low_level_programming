#include "main.h"
#include <string.h>

/**
 * _strcmp - compares strings
 * @s1: string param
 * @s2: string param
 *
 * Return: 0.
 */
int _strcmp(char *s1, char *s2)
{
	int res;

	res = strcmp(s1, s2);

	return (res);
}
